# Crypt of Shadows `v0.1.0`

Statut : `experimental`.

Instantané de la source serveur auditée. La copie du build client VR diverge ; aucune compatibilité client ou runtime précise n'est certifiée.

Les niveaux, densités, limite de temps et récompenses sont déclaratifs. Le serveur doit calculer et valider toute progression ou attribution. Cet instantané peut ne pas encore contenir tous les champs communs décrits dans `TEMPLATE-SPEC.md`.
