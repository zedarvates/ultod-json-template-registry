# Rift du chaos `v0.1.0`

Statut : `experimental`.

Instantané byte-identique aux copies serveur et client VR auditées. Les coordonnées, niveaux, vagues, propriétés, objectifs et récompenses sont des valeurs déclaratives non certifiées qui doivent être autorisées et validées côté serveur.

Aucune compatibilité avec un protocole ou runtime précis n'est certifiée. Cet instantané peut ne pas encore contenir tous les champs communs décrits dans `TEMPLATE-SPEC.md`.
