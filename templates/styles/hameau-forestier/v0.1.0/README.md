# Style hameau forestier `v0.1.0`

Statut : `experimental`.

Instantané byte-identique aux copies serveur et client VR auditées. Les `part_id` sont des identifiants logiques ; les assets, leurs licences, le rendu et l'assemblage runtime ne sont ni inclus ni certifiés.

Cet instantané peut ne pas encore contenir tous les champs communs décrits dans `TEMPLATE-SPEC.md`.
