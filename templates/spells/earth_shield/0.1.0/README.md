# Sort `earth_shield` `v0.1.0`

Statut : `experimental`.

Modele declaratif conforme au schema `templates/schemas/spell/v0.1.0/schema.json`. Ecole `earth`, type `buff`. Les valeurs de degats, delais, couts et effets sont declaratives ; le serveur doit recalculer et valider toute application. Aucun asset, rendu ou equilibrage runtime n'est inclus ou certifie.
