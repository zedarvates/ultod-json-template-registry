# Sort `shadow_bolt` `v0.1.0`

Statut : `experimental`.

Modele declaratif conforme au schema `templates/schemas/spell/v0.1.0/schema.json`. Ecole `shadow`, type `damage_instant`. Les valeurs de degats, delais, couts et effets sont declaratives ; le serveur doit recalculer et valider toute application. Aucun asset, rendu ou equilibrage runtime n'est inclus ou certifie.
