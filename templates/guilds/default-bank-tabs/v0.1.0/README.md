# default-bank-tabs guild template v0.1.0

- Status: experimental
- Source presence: server and VR client build; byte-identical during audit
- Source file: default_bank_tabs.json
- Compatibility: not certified for a client or server release
- Authority: declarative defaults only; every privileged action requires server-side authorization
- Integrity: SHA-256 $hash

This snapshot preserves the source template without modification. Client possession or modification of these values must never grant permissions, bank access, rank changes, currency access, or faction authority.